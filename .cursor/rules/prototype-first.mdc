---
description: 小程序改动前对齐本仓库 prototype 目录中的 HTML 原型
globs: "miniprogram/**/*"
alwaysApply: true
---

# 原型优先（族谱小程序）

- 微信小程序的 HTML 原型在本仓库 **`prototype/`**：**与小程序路由一致的全页面流程（V2）**见 **`prototype/v2/flow.html`**；帧与变量见 **`prototype/shared-styles.css`**，页面与组件样式见 **`prototype/v2/app.css`**。
- 新页面、路由跳转、表单字段：**先更新 `prototype/v2/flow.html` 并确认**，再改 `miniprogram/`。
- 管理后台原型在 **`genealogy` 仓库** 的 `prototype/admin.html`；参数与堂号类需求需与之一致。
- 实现若有偏差，请回写 `prototype/` 与 `genealogy/genealogy-design.md`。
